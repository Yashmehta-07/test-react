import { Stack, Typography } from "@mui/material";

export const TrainingContent = () => {
  return (
    <Stack
      sx={{
        width: "100%",
        background: "red",
      }}
    >
      <Typography color="black">Traning Content</Typography>
    </Stack>
  );
};
